# Hybrid_recommendation_system
How to load dataset and convert text file as dataset to JSON
To access the dataset we can get our data from https://www.aminer.org/billboard/aminernetwork by downloading all three parts of the data now its time to convert the dataset text file into JSON. To learn more about conversion method please checkout text_to_json.py
